# Environment Configuration Example
# Copy this file to .env and fill in the values

# =============================================================================
# CRITICAL SECURITY SETTINGS (REQUIRED IN PRODUCTION)
# =============================================================================

# JWT Secret Key - REQUIRED IN PRODUCTION
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secure-random-secret-key-here

# Admin Credentials - REQUIRED IN PRODUCTION
ADMIN_EMAIL=admin@your-domain.com
ADMIN_PASSWORD=your-very-strong-admin-password-here

# CORS Allowed Origins - REQUIRED IN PRODUCTION
# Comma-separated list (NO localhost in production!)
CORS_ALLOWED_ORIGINS=https://your-app-domain.com

# =============================================================================
# DATABASE
# =============================================================================

DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================

# 1. Generate SECRET_KEY:
#    python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# 2. Set strong ADMIN_PASSWORD (20+ chars, random)
#
# 3. Set CORS to HTTPS domains only
#
# 4. Deploy behind HTTPS reverse proxy

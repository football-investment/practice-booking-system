{
  "code": "league",
  "display_name": "League (Round Robin)",
  "description": "Every player plays against every other player once. Best for smaller groups with balanced competition.",
  "min_players": 4,
  "max_players": 16,
  "requires_power_of_two": false,
  "session_duration_minutes": 90,
  "break_between_sessions_minutes": 15,
  "matches_calculation": "n * (n - 1) / 2",
  "rounds_calculation": "n - 1 if n is even else n",
  "session_naming_pattern": "Round {round_number} - Match {match_number}",
  "placement_rules": {
    "primary": "wins",
    "tiebreakers": ["goal_difference", "goals_scored", "head_to_head"]
  },
  "config_notes": [
    "Each player plays every other player exactly once",
    "Total matches = n*(n-1)/2 where n = player count",
    "Can run multiple matches in parallel if fields available",
    "Fair format - everyone gets equal opportunities"
  ]
}

# Streamlit App Environment Configuration
# Copy this file to .env and fill in the values

# =============================================================================
# API CONFIGURATION (REQUIRED IN PRODUCTION)
# =============================================================================

# FastAPI Backend URL - REQUIRED IN PRODUCTION
# Development: http://localhost:8000
# Production: https://api.your-domain.com
API_BASE_URL=http://localhost:8000

# API Request Timeout (seconds)
API_TIMEOUT=10

# =============================================================================
# ENVIRONMENT
# =============================================================================

# Environment: development, staging, production
ENVIRONMENT=development

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================

# 1. Production MUST use HTTPS:
#    API_BASE_URL=https://api.your-domain.com
#
# 2. The app will CRASH on startup if:
#    - ENVIRONMENT=production AND API_BASE_URL uses http://
#
# 3. Session persistence uses in-memory st.session_state only
#    (no URL query params for security)
